# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets
.env
.env.local
.env.*.local

# Sibling repo — the bulbultypes.xyz marketing site lives here for local
# convenience but is its own git repo and must NEVER be committed to the
# bulbul app repo. Excluded so accidental `git add` can't pull it in.
bulbultypes/
